<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.plugi.plugi">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_logo_splash"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_logo_splash"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".feature.popup.TermsActivity"></activity>
        <activity android:name=".feature.popup.PrivacyActivity" />
        <activity android:name=".feature.popup.HowitWorkActivity" />
        <activity android:name=".feature.popup.ContactActivity" />
        <activity android:name=".feature.popup.AboutActivity" />
        <activity android:name=".feature.main.MainActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation">

        </activity>
        <activity android:name=".feature.resetPassword.ResetPasswordActivity" />
        <activity android:name=".feature.forgetPassword.ForgetPasswordActivity" />
        <activity android:name=".feature.login.LoginActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation">

        </activity>
        <activity android:name=".feature.onBoarding.OnBoardingActivity" />
        <activity android:name=".feature.language.SelectLanguageActivity" />
        <activity android:name=".feature.splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <meta-data
            android:name="com.google.android.gms.wallet.api.enabled"
            android:value="true" />
    </application>

</manifest>